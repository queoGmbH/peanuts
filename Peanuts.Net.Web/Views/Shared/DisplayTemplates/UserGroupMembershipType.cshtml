@using Com.QueoFlow.Peanuts.Net.Core.Infrastructure.Utils
@model Com.QueoFlow.Peanuts.Net.Core.Domain.Users.UserGroupMembershipType?
    
@if (Model.HasValue) {
    @(LabelHelper.EnumToLocalizedString(Model.Value))
} else {
    @("-")
}