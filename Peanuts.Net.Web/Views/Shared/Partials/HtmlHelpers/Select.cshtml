@model IEnumerable<SelectListItem>

<select id="@Guid.NewGuid()" name="@ViewData["name"]">
    @foreach (SelectListItem item in Model) {
        @Html.Partial("Partials/HtmlHelpers/SelectItem", item);
    }
</select>