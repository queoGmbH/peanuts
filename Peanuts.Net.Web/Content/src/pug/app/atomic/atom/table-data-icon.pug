include link

mixin table-data-icon(tableName,columnIndex,item)
    td.webGridCell.webGridCell--icon(webgrid-column-name=tableName + '_column_' + columnIndex webgrid-row-index=item.rowIndex)
        if item.url
            +link(item.url)(class=item.content?'icon-' + item.content:null)
